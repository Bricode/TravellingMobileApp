package op.mobile.app.dev.chalbr1.travelling.helpers

/**
 * I on click listener
 *
 * function that returns the position of the item that was clicked by the user when called
 * @constructor Create empty I on click listener
 */
interface IOnClickListener {
    fun onItemClick(position: Int)
}